import { FC } from "react";

const Admin: FC = () => {
  return (
    <div>
      <h1>Admin Page</h1>
      <p>This is the admin page. Only accessible to administrators.</p>
      {/* Add admin-specific components or functionality here */}
    </div>
  );
};

export default Admin;
